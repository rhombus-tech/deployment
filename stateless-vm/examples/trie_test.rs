use patricia_trie;

fn main() {
    // Print out the module structure of patricia-trie
    println!("Testing patricia-trie crate");
    
    // Try to use the crate directly
    let _trie = patricia_trie::PatriciaTrie::new();
}
